switch("define", "danger")
switch("define", "useMalloc")
switch("define", "noSignalHandler")
switch("define", "nimPreviewSlimSystem")
switch("passC", "-flto")
switch("passL", "-flto")
switch("mm", "arc")
switch("threads", "off")
switch("panics", "on")
switch("opt", "speed")
switch("nimcache", "generated")
switch("genScript", "on")
#switch("cpu", "i386")
switch("os", "openbsd")
switch("gcc.options.linker", "")
switch("clang.options.linker", "")
